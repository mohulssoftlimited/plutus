const config = {
    BACKEND_URL: process.env.REACT_APP_BACKEND_URL || "https://placeholder-backend-url",
    FRONTEND_URL: process.env.FRONTEND_URL || "https://placeholder-frontend-url"
};

export default config;
